import { useParams } from "react-router-dom";

const DriverDetails = () => {

  const params = useParams()

  //http://ergast.com/api/f1/2013/drivers/${parms.driverId}/driverStandings.json
  //http://ergast.com/api/f1/2013/drivers/${parms.driverId}/results.json

  return (
    <div>

    </div>
  );
}
export default DriverDetails;